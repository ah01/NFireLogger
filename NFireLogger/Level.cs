namespace NFireLogger
{
    /// <summary>
    /// Log levels
    /// </summary>
    public enum Level
    {
        // see https://github.com/darwin/firelogger/wiki

        Debug, 
        Info,
        Warning, 
        Error, 
        Critical
    }
}